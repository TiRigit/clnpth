# Projekt: clnpth
# Kopiere zu .env: cp .env.example .env && chmod 600 .env

# Datenbank (Schema in bestehender PostgreSQL-Instanz)
DATABASE_URL=postgresql://postgres:CHANGE_ME@127.0.0.1:5432/postgres?schema=clnpth
DB_SCHEMA=clnpth

# App
PORT=3000
NODE_ENV=development
